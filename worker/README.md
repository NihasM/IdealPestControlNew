# Ideal Pest Control API Worker

Cloudflare Worker for the Ideal Pest Control app API.

## Commands

```sh
npm install
npm run typegen
npm run check
npm run dev
npm run deploy
```

The initial health endpoint is available at `GET /health`.
